# Component — `PageViewTracker`

Logic-only component. Posts a `PageView` event to `/api/meta-conversion` on every pathname change. Returns `null`.

| Side | Path |
|---|---|
| MUI source | `tuitionalFrontend-mui-baseline\src\components\page-view-tracker.tsx` |
| Tailwind port | `tuitionalFrontend\src\components\page-view-tracker.tsx` |

---

## §1 No UI surface — logic-only component, no MUI→Tailwind delta.

Files are byte-for-byte identical. No styling exists in either repo.
