# Component — `PerformanceMonitor`

Logic-only component. Uses `PerformanceObserver` to log LCP / FID / CLS metrics in production. Returns `null`.

| Side | Path |
|---|---|
| MUI source | `tuitionalFrontend-mui-baseline\src\components\performance-monitor.tsx` |
| Tailwind port | `tuitionalFrontend\src\components\performance-monitor.tsx` |

---

## §1 No UI surface — logic-only component, no MUI→Tailwind delta.

Files are byte-for-byte identical. No styling exists in either repo.
